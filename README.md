# Heat Pump Helper

Free heat pump savings calculator for homeowners.

## Local Preview

```bash
npm install
npm run dev
```

## GitHub Pages

This project is ready for GitHub Pages. Push it to a repository with the default branch named `main`, then enable Pages with **Source: GitHub Actions**.

The deploy workflow builds the app and publishes the `dist` folder automatically.
